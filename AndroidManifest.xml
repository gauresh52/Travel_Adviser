<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCT3-lL68glxCydrd3zN33_kdwL71hBG6U"/>
        <activity
            android:name=".ui.home.HomeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Search Activity -->
        <activity
            android:name=".ui.search.SearchActivity"
            android:exported="true"
            android:label="@string/search_label" />

        <activity
            android:name=".ui.calender.CalenderActivity"
            android:exported="true"
            android:label="@string/calender" />
        <activity
            android:name=".ui.trips.TripSelectionActivity"
            android:exported="true"
            android:label="@string/tripsection" />
        <activity
            android:name=".ui.recommendations.RecommendationsActivity"
            android:exported="true"
            android:label="@string/tripsection" />

    </application>

    <uses-permission android:name="android.permission.INTERNET"/>

    </manifest>